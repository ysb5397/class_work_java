package ch06;

// 클래스를 설계하는 측
public class Warrior {
    // 멤버 변수
    // 멤버 변수를 초기화 하지 않으면
    // 인스턴스화 될 때 기본 값으로 초기화 된다.
    // 속성 설계
    String name;
    int hp;
    int power;
    int dp;
    String color;
    double weight;
    boolean isDie;
}
